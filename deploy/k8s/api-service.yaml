apiVersion: v1
kind: Service
metadata:
  name: atlas-api
  namespace: atlas-bot
  labels:
    app: atlas-api
    component: backend
spec:
  type: ClusterIP
  selector:
    app: atlas-api
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
