apiVersion: v1
kind: Service
metadata:
  name: atlas-web
  namespace: atlas-bot
  labels:
    app: atlas-web
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: atlas-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
