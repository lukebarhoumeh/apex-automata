# Apex Automata — ML pipeline dependencies
# Pinned minor versions; bump deliberately when training a new model
# (mismatched ONNX runtime versions in production cause silent inference drift).

# Data + database
pandas>=2.2.0,<3.0.0
pyarrow>=15.0.0,<20.0.0
supabase>=2.3.0,<3.0.0
python-dotenv>=1.0.0

# ML core
scikit-learn>=1.4.0,<2.0.0
xgboost>=2.0.0,<3.0.0
optuna>=3.5.0,<4.0.0

# ONNX export for runtime inference
onnx>=1.15.0,<2.0.0
onnxmltools>=1.12.0,<2.0.0
skl2onnx>=1.16.0,<2.0.0
onnxruntime>=1.17.0,<2.0.0

# Evaluation + visualization (reports)
matplotlib>=3.8.0,<4.0.0
seaborn>=0.13.0,<1.0.0

# Quality of life
tqdm>=4.66.0
tabulate>=0.9.0
