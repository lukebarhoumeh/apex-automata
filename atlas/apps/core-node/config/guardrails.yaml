# DO NOT EDIT — this file is NOT read by the runtime.
#
# The single source of truth for every risk limit, fee schedule, filter and
# strategy parameter is:
#
#     atlas/config/guardrails.yaml
#
# `loadGuardrails()` (atlas/apps/core-node/src/config/loadGuardrails.ts)
# resolves that path relative to its own module location and throws if a
# caller points it anywhere else, so any value placed in this file is never
# loaded. This stub exists only so that anyone landing here from an old
# bookmark, grep hit or muscle memory is redirected instead of editing a
# copy that silently diverges (which is exactly what this file used to be).
#
# CI: `pnpm check:config` (src/config/config-drift.ts) fails if this file
# gains any runtime keys, and if any other guardrails.yaml appears in the
# tree that is not a stub like this one.
DO_NOT_EDIT: true
canonical: atlas/config/guardrails.yaml
