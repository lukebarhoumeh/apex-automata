{
  "$schema": "./strategies.schema.json",
  "description": "DEPRECATED — NOT READ BY THE RUNTIME. Strategy enable/disable state and every strategy parameter live ONLY in atlas/config/guardrails.yaml (disabled_strategies, momentum, per_symbol/perps_symbols/hyperliquid_symbols strategy_overrides). The `enabled` flags below are a mirror kept for anyone who still opens this file; `pnpm check:config` (atlas/apps/core-node/src/config/config-drift.ts) fails CI if they disagree with guardrails.yaml or if any parameter key is added here.",
  "version": "2.0.0",

  "strategies": {
    "momentum": {
      "enabled": true
    },
    "trend_follow": {
      "enabled": true
    },
    "vwap_mr": {
      "enabled": false
    },
    "breakout": {
      "enabled": false
    }
  },

  "registry": {
    "maxErrorsBeforeDisable": 5,
    "signalDedupeWindowMs": 300000
  },

  "global": {
    "parallelExecution": false,
    "logSignals": true,
    "persistSignals": true
  }
}
